<h1>ユーザ一覧</h1>
<div class="container">
  <% @users.each do |user| %>
    <div class="card">
      <div class="card-body">
        <h4 class="card-title">
          <%= link_to user.name+'さんとトーク', talk_path(user.id) %>
        </h4>
      </div>
    </div>
  <% end %>
</div>